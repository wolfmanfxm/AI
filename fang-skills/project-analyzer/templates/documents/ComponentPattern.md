# Component Patterns

> 生成日期：{{date}} | 项目：{{project}} | 版本：{{version}}

## 1. 全局通用组件

| 组件 | 路径 | 用途 | 关键 Props | 复用度 | 引用次数 |
|------|------|------|-----------|--------|---------|
| ...  | ...  | ...  | ...       | 高/中/低 | N       |

### 组件详情

对每个高复用组件展开：

#### {{组件名}}
- **路径**：`{global_components_dir}/{{name}}.vue`
- **用途**：一句话描述
- **Props**：
  ```ts
  // 实际 Props 类型定义（从 defineProps 原样提取）
  ```
- **Emits**：事件名和参数
- **Slots**：插槽名和用途
- **Expose**：暴露的方法（如有）
- **引用位置**：使用该组件的文件列表（前 5 个）
- **业务耦合度**：是否直接 import 业务 API / 全局 Store
- **示例**：来自 `{views_dir}/模块名/页面.vue:行号` 的实际使用代码

## 2. 页面/模块级组件

| 组件 | 当前位置 | 用途 | 跨模块引用 |
|------|---------|------|-----------|
| ...  | ...     | ...  | 是/否     |

## 3. 候选提升组件

被 2 个以上模块引用但尚未提升到全局组件目录的组件：

| 组件 | 当前位置 | 引用模块 | 建议目标路径 | 提升阻碍 |
|------|---------|---------|-------------|---------|
| ...  | ...     | A, B, C | {global_components_dir}/ | 耦合原因 |

## 4. 框架层组件

项目框架层/基础设施层提供的全局组件：

| 组件 | 用途 | 是否被业务层使用 |
|------|------|-----------------|
| ...  | ...  | 是/否           |
