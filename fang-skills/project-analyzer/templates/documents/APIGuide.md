# API Guide

> 生成日期：{{date}} | 项目：{{project}} | 版本：{{version}}

## Directory Structure

当前项目 API 层的目录树（从实际目录扫描得出）。

## Request Functions

### 命名约定

从实际代码中统计 API 函数的命名风格（如 `verbNoun`、`verbNounUsingMethod` 等）及各自占比。

### 函数签名模板

从实际代码提取典型 API 函数的签名模板，展示参数和返回值类型。

### 参数约定
- 路径参数的处理方式
- Query 参数的处理方式
- Body 参数的处理方式
- 文件上传的实现方式

## Request Config

### URL 前缀
- 基础 URL 配置方式（环境变量或硬编码）

### 默认配置
- `timeout` 实际值
- `headers` 默认头
- `withCredentials` 设置

### 特殊配置
- 请求加密标记和方式
- 响应格式转换
- 特殊响应类型（`blob` 下载等）

## Response Handling

### 响应结构
从实际代码提取的标准响应格式（interface/type 定义）。

### 成功处理
- 数据提取方式和层级路径
- 分页响应中列表数据的实际路径

### 错误处理
- 全局拦截器位置和行为
- HTTP 状态码 → 用户提示的映射
- 业务错误码的处理方式
- Token 刷新逻辑

## Pagination

### 请求参数
- 页码字段的实际命名
- 每页大小字段的实际命名

### 响应字段
- 列表数据的实际路径
- 总数字段名
- 总页数字段名

### 分页组件
- 项目中使用的分页组件及默认配置

## Encryption

记录项目中实际使用的加密方式（如有）：
- 加密算法
- 触发条件
- 白名单机制
- 在请求拦截器中的位置

## Error Codes

常见业务错误码及其处理方式（从实际代码和拦截器中提取）：

| 错误码 | 含义 | 处理方式 | 来源 |
|--------|------|---------|------|
| ...    | ...  | ...     | ...  |
