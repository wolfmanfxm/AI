# Component Patterns

> 生成日期：{{date}} | 项目：{{project}} | 版本：{{version}}

## 1. 业务通用组件（workspace/components/）

| 组件 | 路径 | 用途 | 关键 Props | 复用度 | 引用次数 |
|------|------|------|-----------|--------|---------|
| ...  | ...  | ...  | ...       | 高/中/低 | N       |

### 组件详情

对每个组件展开：

#### {{组件名}}
- **路径**：`workspace/components/{{name}}.vue`
- **用途**：一句话描述
- **Props**：
  ```ts
  // 实际 Props 类型定义
  ```
- **Emits**：事件名和参数
- **Slots**：插槽名和用途
- **defineExpose**：暴露的方法（如有）
- **引用位置**：使用该组件的文件列表（前 5 个）
- **业务耦合度**：是否直接 import 业务 API / Store
- **示例**：来自 `workspace/views/xxx/index.vue:42-55` 的实际使用代码

## 2. 页面级组件（workspace/views/*/components/）

| 组件 | 当前位置 | 用途 | 跨模块引用 |
|------|---------|------|-----------|
| ...  | ...     | ...  | 是/否     |

## 3. 候选提升组件

被 2 个以上模块引用但尚未提升到 `workspace/components/` 的组件：

| 组件 | 当前位置 | 引用模块 | 建议目标路径 | 提升阻碍 |
|------|---------|---------|-------------|---------|
| ...  | ...     | A, B, C | workspace/components/ | 耦合业务 Store |

## 4. 框架层组件（src/components/）

| 组件 | 用途 | 是否被 workspace 使用 |
|------|------|---------------------|
| ...  | ...  | 是/否               |
