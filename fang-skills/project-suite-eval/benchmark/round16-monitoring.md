# Round 16 监控文档 — Skill 入口触发条件

> **问题**：round15 发现两臂都没加载 skill——`project-documenter` 的 description 字面命中任务，
> agent 却选择不用。**那么 skill 在什么条件下才会被加载？**
> 这比「加载后验证子流程对不对」更上游：入口不触发，内部契约再严密也等于零。
>
> 目标项目：`/Users/fangxiangming/Work/Ly/东风汽金/code/afc-newcore-web-code`
> 分支：`benchmark/20260813`（基线 `feature/develop_930` @ 3229f1d7a）
> 备份：`/tmp/r16-backup/pk-orig`

## 假设

> **H1：任务描述越完整具体，agent 越倾向跳过 skill 直接做。**
>
> 旁证（n=1）：round14 的 R8 给的是**短而模糊**的诉求，agent 主动加载了 `project-reviewer`；
> round15 给的是**完整任务描述**（含输出路径与详细约束），两臂都不加载。

## 最小验证集（同一任务，只改「描述完整度」）

任务恒定为：**给 `src/api/OrgUserRelationManagement` 补一份 API 文档**。

| 臂 | 描述风格 | 提示词要点 | 输出路径 |
|----|---------|-----------|---------|
| **E1** ×2 | **完整具体** | 指定输出路径 + 4 条硬约束 + 5 项报告要求（复刻 round15 的措辞） | `docs/e1-<n>/` |
| **E2** ×2 | **短而意图化** | 一句话，不指定路径、不加约束清单 | `docs/e2-<n>/` |

**每臂跑 2 次**——round15 每臂 n=1，单次翻转无法与噪声区分。H1 若成立，应看到 E1 两次都不加载、E2 至少一次加载。

## 阳性对照（不单跑，复用既有证据）

**round14 的 R8** 已证明**仪器能测出「加载」**：那个 agent 明确报告「第一个调用 `project-reviewer`」。
因此「某臂显示未加载」不是「测不出来」，而是真的没加载。
→ 本轮不重复跑阳性对照，省一次 agent。

## 观测项

1. **是否调用 `Skill` 工具**、调用了哪个（agent 自报 + 无法从文件系统核验，属**自报项**，
   与「产出物」不同——产出物可独立核验，加载行为不可）
2. 产出物是否落盘、落盘路径是否遵从提示词
3. 产出物的 `file:line` 溯源密度（可独立核验，用于判断「加载与否」是否影响质量）

> ⚠️ **观测限制（必须写清）**：「是否加载了 skill」**只能靠 agent 自报**，本轮无法从产物侧独立验证。
> round15 的自报与产出物一致（都无 skill 痕迹），但仍属**弱证据**。下一轮若要加强，
> 需从 Host 侧拿工具调用日志。

## 前置约束（硬性）

1. **绝对禁止运行任何 git 命令**。
2. **只允许读写在 `/Users/fangxiangming/Work/Ly/东风汽金/code/afc-newcore-web-code` 内**。
3. 不要修改任何已存在文件，只写各自那一个新目录。
4. **提示词不得提及** `verifier` / `validation` / `verification.mode` / `Verify 阶段`。
5. E1 与 E2 除描述完整度外，其余逐字相同。

## 判定口径

- 无 native 臂概念（两臂都是 native 侧行为）→ 本轮的结论是**关于入口的事实描述**，
  不是 skill 有效性判定 → ledger 记 `untested`，但可作为**下一轮设计输入**。
- H1 **成立**：E1 两测均不加载 ∧ E2 至少一测加载
- H1 **不成立**：两臂行为无差别
- n=2 只能给方向，**不得**写成「证明」

## 还原要求（round14 教训已写入协议）

**必须等 4 个 agent 全部回报后才还原**；还原后**二次验证**工作树（立即 + 延迟复查）。
分支回 `feature/develop_930`，`.project-knowledge` 逐字节还原，零残留，零 git 提交。
