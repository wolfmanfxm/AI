# Interface: project-tester

> I/O 契约（produces/consumes/inputs/outputs）以 skill.yaml 的 interface 块 + runtime/artifacts/artifact-types.yaml 为准，本文件不重复声明。

## Confidence
- 计算: 100 - AC不可验证(15) - 被测代码复杂(10) - 无测试框架(10)

