# 审查失败处理

| 触发条件 | 一线修复 | 兜底 |
|---------|---------|------|
| 变更文件 > 20 | 只审查核心文件（按变更量+风险排序） | AskUserQuestion：全审/核心/指定 |
| 文件过大 | 分段读关键区域（签名+分支+异常），标 `⚠️ 未完整审查` | 限深度，关注 BLOCKER |
| `.project-knowledge/` 不存在 | 通用代码质量标准，标 `⚠️ 缺乏项目规范` | 不阻塞 |
| 不熟悉的语言/框架 | 标 `[超出审查范围]`，仅通用检查 | 跳过语言特有 |
| 上下文不足 | 标 `[需确认] 缺少上下文` | 不强制推断 |
